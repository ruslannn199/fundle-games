import { Flex, Form } from 'antd';
import styled from 'styled-components';

export const SignInActionsWrapper = styled(Flex)`
  margin: 0;
`;

export const SignInRecoveryWrapper = styled(Form.Item)`
  margin: .5rem;
`;
