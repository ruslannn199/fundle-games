import { orange } from '../../utils/themes';
import { LoadingOutlined } from '@ant-design/icons';

const Spinner = <LoadingOutlined style={{ fontSize: 48, color: orange }} spin />;

export default Spinner;
