import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const CartPopupLink = styled(Link)`
  display: flex;
  position: relative;
  color: #000;
  height: 6.4rem;
  
  &:hover {
    color: #000;
  }
`;
